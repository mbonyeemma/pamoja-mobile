import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    display: 'flex',
    width: '90%',
    // borderWidth: 1,
    flexDirection: 'row',
    height: '9%',
    marginTop: 10
  }
});
